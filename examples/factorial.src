
fn factorial(int n) -> int {
    int result = 1;
    
    while (n > 0) {
        result = result * n;
        n = n - 1;
    }
    
    return result;
}

fn main() -> int {
    int num = 5;
    int fact = factorial(num);
    return fact;
}