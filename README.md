# MiniCompiler — Учебный компилятор для MiniLang

### Ключевые особенности

#### Спринт 1: Лексический анализатор ✅
- Полноценный лексический анализатор с поддержкой всех типов токенов
- Препроцессор с поддержкой макросов и условной компиляции (`#define`, `#ifdef`, `#if`, `#elif`, `#else`, `#endif`)
- Подробная обработка ошибок с указанием позиции и кода ошибки
- 36 автоматических тестов (25 валидных + 11 с ошибками)
- Производительность O(n), поддержка файлов до 1 МБ
- Поддержка escape-последовательностей в строках (`\n`, `\t`, `\r`, `\"`, `\\`)
- Вложенные комментарии `/* /* */ */`
- Восстановление после ошибок (panic mode)

#### Спринт 2: Синтаксический анализатор ✅
- Парсер рекурсивного спуска с поддержкой LL(1) грамматики
- Полная иерархия узлов AST (Abstract Syntax Tree)
- Поддержка всех операторов с правильным приоритетом и ассоциативностью
- Объявления функций, структур и переменных
- Управляющие конструкции: `if-else`, `while`, `for`, `return`
- Вызовы функций с аргументами
- Индексация массивов `arr[10]`
- Доступ к полям структур `p.x`
- Постфиксные операторы `x++`, `x--`
- Составные присваивания `+=`, `-=`, `*=`, `/=`, `%=`
- Визуализация AST в текстовом формате, Graphviz DOT и JSON
- Интеграция с лексером и препроцессором
- 19 тестов парсера (9 валидных + 10 с синтаксическими ошибками)

#### Спринт 3: Семантический анализ ✅
- Таблица символов с поддержкой вложенных областей видимости
- Проверка объявлений (дублирование, корректность типов)
- Проверка типов выражений и операций
- Проверка вызовов функций (количество и типы аргументов)
- Проверка оператора return (соответствие типу функции)
- Проверка условий в управляющих конструкциях (if, while, for)
- Проверка доступа к массивам и структурам
- **Memory Layout** — вычисление смещений переменных в стеке
- Подробные сообщения об ошибках на русском языке с подсказками по исправлению
- 15 тестов семантического анализа (5 валидных + 10 с ошибками)

#### Спринт 4: Промежуточное представление (IR) ✅
- Трехадресный код (Three-Address Code)
- Базовые блоки и граф потока управления (CFG)
- Поддержка арифметических операций: `ADD`, `SUB`, `MUL`, `DIV`, `MOD`
- Поддержка сравнений: `CMP_EQ`, `CMP_NE`, `CMP_LT`, `CMP_LE`, `CMP_GT`, `CMP_GE`
- Логические операции: `AND`, `OR`, `NOT`
- Управление потоком: `JUMP`, `JUMP_IF`, `JUMP_IF_NOT`, `LABEL`
- Вызовы функций: `CALL`, `PARAM`, `RETURN`
- Работа с памятью: `LOAD`, `STORE`, `MOVE`, `ALLOCA`
- **SSA форма (Static Single Assignment)** — каждая переменная присваивается ровно один раз
- Переименование переменных с версионированием (`x0`, `x1`, `x2`...)
- Визуализация IR в текстовом формате
- 15 тестов IR (12 валидных + 3 с ошибками)

---

## Общая статистика проекта

| Компонент | Валидные тесты | Тесты с ошибками | Всего |
|-----------|:--------------:|:----------------:|:-----:|
| Лексер | 25 | 11 | **36** |
| Парсер | 6 | 10 | **16** |
| Семантика | 5 | 10 | **15** |
| IR | 12 | 3 | **15** |
| **ИТОГО** | **48** | **34** | **82** |

**Покрытие тестами: 82 теста, 100% прохождение** ✅

---

## Быстрый старт

### Установка и сборка
```bash
# Клонирование репозитория
cd minicompiler

# Сборка проекта
make clean
make

# Проверка работы
./minicompiler help
```

### Первая программа
Создайте файл `hello.mini`:
```c
// Моя первая программа на MiniLang
fn main() -> int {
    int x = 42;
    string msg = "Hello, World!";
    return x;
}
```

Запустите компиляцию:
```bash
# Только лексер (показать токены)
./minicompiler lex hello.mini

# Парсер с выводом AST
./minicompiler parse hello.mini

# Семантический анализ с таблицей символов
./minicompiler check hello.mini --verbose

# Генерация IR (трехадресный код)
./minicompiler ir hello.mini

# Генерация SSA формы
./minicompiler ssa hello.mini

# Полная компиляция (препроцессор + лексер + парсер + семантика)
./minicompiler compile hello.mini

# Сгенерировать DOT для визуализации AST
./minicompiler parse hello.mini --format dot --output ast.dot
dot -Tpng ast.dot -o ast.png
```

---

## Структура проекта

```
minicompiler/
├── docs/                      # Документация
│   ├── grammar.md            # Формальная LL(1)-грамматика
│   ├── language_spec.md      # Спецификация языка
│   ├── LEXER_README.md       # Документация лексера
│   ├── PARSER_README.md      # Документация парсера
│   └── SEMANTIC_README.md    # Документация семантического анализатора
├── examples/                  # Примеры кода
│   ├── arrays.src            # Работа с массивами
│   ├── factorial.src         # Рекурсивный и итеративный факториал
│   ├── hello.src             # Базовый пример
│   └── preprocess_test.src   # Тест препроцессора
├── include/                   # Заголовочные файлы
│   ├── lexer/                # Лексический анализатор
│   │   ├── Scanner.hpp
│   │   ├── Token.hpp
│   │   └── TokenType.hpp
│   ├── parser/               # Синтаксический анализатор
│   │   ├── AST.hpp
│   │   ├── ASTVisitor.hpp
│   │   ├── ASTPrettyPrinter.hpp
│   │   ├── ASTDotGenerator.hpp
│   │   ├── ASTJsonGenerator.hpp
│   │   └── Parser.hpp
│   ├── preprocessor/         # Препроцессор
│   │   ├── Preprocessor.hpp
│   │   └── PreprocessorFrontend.hpp
│   ├── semantic/             # Семантический анализатор
│   │   ├── SemanticAnalyzer.hpp
│   │   └── SymbolTable.hpp
│   ├── ir/                   # Промежуточное представление
│   │   ├── IR.hpp
│   │   ├── IRGenerator.hpp
│   │   ├── SSA.hpp
│   │   └── SSABuilder.hpp
│   └── utils/                # Утилиты
│       ├── ErrorCodes.hpp
│       └── ErrorReporter.hpp
├── scripts/                   # Вспомогательные скрипты
│   ├── remove_bom.sh         # Удаление BOM из файлов
│   └── run_tests_simple.sh   # Запуск тестов
├── src/                       # Исходный код
│   ├── lexer/                 # Реализация лексера
│   ├── parser/                # Реализация парсера
│   ├── preprocessor/          # Реализация препроцессора
│   ├── semantic/              # Реализация семантического анализатора
│   ├── ir/                    # Реализация IR и SSA
│   ├── utils/                 # Реализация утилит
│   └── main.cpp               # Точка входа
├── tests/                     # Тесты (82 теста)
│   ├── lexer/                 # Тесты лексера (Спринт 1)
│   │   ├── invalid/           # 11 тестов с ошибками
│   │   └── valid/             # 25 валидных тестов
│   ├── parser/                # Тесты парсера (Спринт 2)
│   │   ├── invalid/           # 10 тестов с ошибками
│   │   └── valid/             # 6 валидных тестов
│   ├── semantic/              # Тесты семантики (Спринт 3)
│   │   ├── invalid/           # 10 тестов с ошибками
│   │   └── valid/             # 5 валидных тестов
│   └── ir/                    # Тесты IR (Спринт 4)
│       ├── invalid/           # 3 теста с ошибками
│       ├── valid/             # 12 валидных тестов
│       └── ssa/               # Тесты SSA формы
│           └── valid/         # SSA тесты
├── Makefile                   # Система сборки
├── README.md                  # Этот файл
└── .gitignore                 # Игнорируемые файлы
```

---

## Команды и использование

### Основные команды
```bash
# Токенизация файла (только лексер)
./minicompiler lex <file>

# Только препроцессор
./minicompiler preprocess <file>

# Парсинг и построение AST
./minicompiler parse <file> [options]

# Семантический анализ
./minicompiler check <file> [options]

# Генерация IR (трехадресный код)
./minicompiler ir <file> [options]

# Генерация SSA формы
./minicompiler ssa <file> [options]

# Полная компиляция (препроцессор + лексер + парсер + семантика)
./minicompiler compile <file>

# Справка
./minicompiler help
```

### Опции для команд parse/check/ir/ssa
```bash
--format <text|dot|json>   # Формат вывода (по умолчанию: text)
--output <file>            # Выходной файл (по умолчанию: stdout)
--verbose                  # Показать подробную информацию
--stats                    # Показать статистику компиляции
```

### Makefile цели
| Команда | Описание |
|---------|----------|
| `make all` | Собрать все компоненты |
| `make clean` | Очистить объектные файлы |
| `make build-tests` | Собрать все тестовые раннеры |
| `make test-lexer` | Запустить тесты лексера (Спринт 1) |
| `make test-parser` | Запустить валидные тесты парсера |
| `make test-parser-errors` | Запустить тесты парсера с ошибками |
| `make test-parser-all` | Все тесты парсера (Спринт 2) |
| `make test-semantic` | Запустить тесты семантики (Спринт 3) |
| `make test-ir` | Запустить тесты IR (Спринт 4) |
| `make test-ssa` | Запустить тесты SSA формы |
| `make test-all` | Запустить все 82 теста |
| `make check-all` | Проверить все примеры из examples/ |
| `make ast-file FILE=x` | Визуализация AST для указанного файла |
| `make run-lexer FILE=x` | Запустить лексер на файле |
| `make run-parser FILE=x` | Запустить парсер на файле |
| `make run-check FILE=x` | Запустить семантику на файле |
| `make run-ir FILE=x` | Запустить IR на файле |
| `make run-ssa FILE=x` | Запустить SSA на файле |
| `make help` | Показать справку |

---

## Визуализация AST

```bash
# Установите Graphviz (если не установлен)
sudo apt install graphviz   # для Ubuntu

# Сгенерировать DOT файл
./minicompiler parse examples/factorial.src --format dot --output factorial.dot

# Создать PNG
dot -Tpng factorial.dot -o factorial.png

# Открыть изображение
eog factorial.png

# Или используйте Makefile цели
make ast-file FILE=examples/factorial.src
```

---

## Примеры вывода IR и SSA

### Трехадресный код (IR)
```
# MiniLang IR Program

function main: int ()

entry:
  t0 = MUL 3, 4
  t1 = ADD 2, t0
  [x] = MOVE t1
  RETURN [x]
```

### SSA форма
```
# MiniLang SSA IR Program

function main: int ()

entry:
  x00 = MOVE 1
  y00 = MOVE 2
  t0 = CMP_GT x00, 0
  JUMP_IF t0, then_1
  JUMP else_2

then_1:
  x10 = MOVE 3
  JUMP endif_3

else_2:
  JUMP endif_3

endif_3:
  t4 = ADD x10, y00
  RETURN t4
```

---

## Примеры программ

### Пример 1: Факториал
```c
fn factorial(int n) -> int {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

fn main() -> int {
    return factorial(5);
}
```

### Пример 2: Структуры
```c
struct Point {
    int x;
    int y;
}

fn main() -> int {
    Point p;
    p.x = 10;
    p.y = 20;
    return p.x + p.y;
}
```

### Пример 3: Циклы
```c
fn sum(int n) -> int {
    int result = 0;
    int i = 1;
    while (i <= n) {
        result = result + i;
        i = i + 1;
    }
    return result;
}
```

---

## Дорожная карта

### ✅ Спринт 1 (Завершен)
- [x] Лексический анализатор
- [x] Препроцессор с макросами
- [x] 36 автоматических тестов
- [x] Обработка ошибок с восстановлением

### ✅ Спринт 2 (Завершен)
- [x] Формальная LL(1)-грамматика
- [x] Парсер рекурсивного спуска
- [x] Полная иерархия AST узлов
- [x] Поддержка структур и массивов
- [x] Визуализация AST (текст, DOT, JSON)
- [x] 16 тестов парсера

### ✅ Спринт 3 (Завершен)
- [x] Таблица символов с областями видимости
- [x] Проверка типов и объявлений
- [x] Проверка вызовов функций
- [x] Проверка оператора return
- [x] Memory Layout (stack offsets)
- [x] 15 тестов семантического анализа

### ✅ Спринт 4 (Завершен)
- [x] Трехадресный код (IR)
- [x] Базовые блоки и CFG
- [x] SSA форма с переименованием переменных
- [x] 15 тестов IR

---

## Документация
- [Спецификация языка MiniLang](docs/language_spec.md)
- [Формальная грамматика](docs/grammar.md)
- [Документация лексера](docs/LEXER_README.md)
- [Документация парсера](docs/PARSER_README.md)
- [Документация семантического анализатора](docs/SEMANTIC_README.md)


